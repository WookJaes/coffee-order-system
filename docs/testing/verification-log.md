# Verification Log

| 날짜         | 작업 | 레벨 | 결과 | 명령 또는 근거 | 비고                                                                                                             |
|------------| --- | --- | --- | --- |----------------------------------------------------------------------------------------------------------------|
| YYYY-MM-DD | 작업명 | Unit/API/DB Integration/Infra Integration/Manual HTTP/Load | PASS/FAIL/PARTIAL | 실제 실행 명령 또는 산출물 경로 | 결과와 남은 위험                                                                                                      |
| 2026-07-13 | Issue #1 프로젝트 기반 문서 계약 정합성 점검 | API/Manual HTTP/Infra Integration | PASS | `gh issue view 1 --json title,body,url,state,labels`<br>`rg -n -e 'coffee-menus' -e '"code"' -e '오류 코드' README.md docs .github AGENTS.md -g '!docs/testing/verification-log.md'`<br>`rg -n -e 'GET /api/menus' -e 'GET /api/menus/popular' -e '"status": 201' -e '내부 ErrorCode' -e '실패 응답 JSON에는 예외 코드를 포함하지 않고' -e 'HTTP 상태와 응답 메시지' README.md docs .github AGENTS.md`<br>`docker compose config` | 이전 API 경로(`/api/coffee-menus`)와 실패 응답의 `code` 필드, `오류 코드` 문구가 검증 로그를 제외한 문서에 남아 있지 않음을 확인했다. 현재 계약은 성공 시 `status/message/data`, 실패 시 `status/message`이며, Compose 설정 렌더링도 성공했다. |
| 2026-07-14 | Issue #2 Flyway 스키마와 메뉴 Seed 구성 | DB Integration/Infra Integration/Unit | PASS | Issue #2 확인<br>검증용 빈 MySQL 기동: `docker compose --env-file .env.example -p coffee_issue2_schema up -d mysql`<br>Flyway 적용: `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun`<br>DB 메타데이터 확인: schema history, 테이블, 제약, 인덱스, seed 메뉴 조회<br>seed 재실행 가능성 확인: `V2__seed_active_menus.sql` 재실행 후 메뉴 건수 조회<br>전체 테스트: `./gradlew test` | 별도 Compose project(`coffee_issue2_schema`)의 새 MySQL 볼륨에서 Flyway v1/v2가 성공 적용됐다. `users`, `menus`, `points`, `point_histories`, `orders`, `order_events`와 `flyway_schema_history`가 생성됐고, `points(user_id)`, `orders(user_id, idempotency_key)`, `order_events(order_id)` 유니크 제약 및 상태/금액/잔액 CHECK, FK, 주요 조회 인덱스를 확인했다. seed 재실행 후 ACTIVE 메뉴는 5건으로 유지됐다. `./gradlew test`는 BUILD SUCCESSFUL이었다. |
| 2026-07-14 | Issue #3 메뉴 목록 조회 API | Unit/API/DB Integration/Manual HTTP | PASS | `./gradlew test --rerun-tasks --console=plain`<br>검증용 MySQL: `docker compose --env-file .env.example -p coffee_issue3_menu up -d mysql`<br>로컬 실행: `.env.example` 값을 프로세스 환경으로만 주입한 `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun`<br>`curl http://localhost:8080/api/menus` 및 MySQL `menus` 조회 | 전체 테스트가 BUILD SUCCESSFUL이었다. Repository 테스트에서 `ACTIVE`만 조회되고 `SOLD_OUT`이 제외됨을 확인했다. 새 MySQL에서 Flyway v1/v2가 적용된 뒤 seed `ACTIVE` 메뉴 5건과 `GET /api/menus`의 `status/message/data(menuId,name,price)` 응답을 확인했다. 메뉴 등록·수정, 포인트·주문, Redis·Kafka는 제외 범위이며, 검증 중 `.env`와 `.env.example`의 DB 사용자 차이가 있어 `.env`는 수정하지 않고 후자를 실행 프로세스에만 주입했다. |
| 2026-07-14 | Issue #4 포인트 충전 API | Unit/API/DB Integration/Manual HTTP | PASS | `./gradlew test --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.point.controller.PointControllerTest --console=plain`<br>`./gradlew test --tests com.example.coffeeordersystem.point.entity.PointEntityMappingTest --console=plain`<br>`./gradlew test --tests com.example.coffeeordersystem.point.dto.PointChargeResponseTest --console=plain`<br>`./gradlew test --rerun-tasks --console=plain`<br>`SPRING_PROFILES_ACTIVE=local ./gradlew bootRun`<br>`POST /api/points/charge` 및 MySQL `points`, `point_histories` 조회 | focused test와 전체 테스트는 모두 `BUILD SUCCESSFUL`이었다. 신규 포인트 생성, 기존 잔액 누적, 0 이하·null·100,000 초과 금액, null 사용자 ID, 존재하지 않는 사용자, `CHARGE` 이력, User-Point 1:1 및 User 연관관계 LAZY 매핑을 검증했다. request record의 Bean Validation이 서비스 호출 전 amount에는 `INVALID_CHARGE_AMOUNT`, userId에는 `INVALID_USER_ID` 400 공통 응답으로 변환되는 것도 MockMvc로 확인했다. Docker MySQL의 `coffee` 스키마에 검증 사용자(ID 1)를 만들고 10,000P를 충전해 HTTP 200 응답과 `points.balance=10000`, `point_histories(type=CHARGE, amount=10000, balance_after=10000)` 저장을 확인했다. 포인트 환불·주문/결제·Redis·Kafka는 제외 범위다. |
| 2026-07-14 | Issue #4 테스트 사용자 Flyway Seed | DB Integration/Manual HTTP | PASS | `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun`<br>MySQL `flyway_schema_history`, `users` 조회 | V3 `seed_test_user`가 적용됐고, `users.id=1` 테스트 사용자가 생성됨을 확인했다. |
| 2026-07-14 | Issue #4 독립 리뷰 보완 | Unit/API/Concurrency | PASS | 보완 전: `./gradlew test --tests '*PointControllerTest' --tests '*PointServiceTest' --no-daemon --console=plain`<br>보완 후: 같은 명령 재실행 | 보완 전에는 숫자가 아닌 `amount`가 500으로 처리되고, 동시 충전 시 `points.user_id` 유니크 제약 위반이 재현됐다. 사용자 행에 PESSIMISTIC_WRITE 잠금을 적용하고 `HttpMessageNotReadableException`을 400 공통 응답으로 변환한 뒤, 숫자가 아닌 금액과 같은 사용자 5건 동시 충전(잔액 5,000·CHARGE 이력 5건) 회귀 테스트가 통과했다. |
| 2026-07-14 | Issue #4 역직렬화 오류 응답 정합성 | API/Unit | PASS | 보완 전: `./gradlew test --tests '*PointControllerTest' --no-daemon --console=plain`<br>보완 후: `./gradlew test --tests '*PointControllerTest' --no-daemon --console=plain`<br>전체: `./gradlew test --rerun-tasks --no-daemon --console=plain` | 숫자가 아닌 `amount`·`userId`, 문법이 잘못된 JSON이 기존에는 금액 전용 메시지로 처리됨을 재현했다. 모든 `HttpMessageNotReadableException`을 일반적인 `요청 본문 형식이 올바르지 않습니다.` 400 공통 응답으로 변환한 뒤 controller 9건과 전체 테스트가 모두 통과했다. |
| 2026-07-14 | Issue #5 주문 및 포인트 결제 API | Unit/API/DB Integration | PASS | RED: `./gradlew test --tests '*OrderEntityMappingTest'`, `*OrderCreateResponseTest`, `*OrderServiceTest`, `*OrderControllerTest`<br>Focused: `./gradlew test --tests '*OrderServiceTest' --tests '*OrderControllerTest' --console=plain`<br>전체: `./gradlew test --console=plain` | RED 단계에서 주문 엔티티·DTO·서비스·Controller 부재에 따른 예상 컴파일 실패를 확인했다. 이후 focused test와 전체 테스트가 모두 `BUILD SUCCESSFUL`이었다. `OrderServiceTest`는 정상 결제, 메뉴·사용자·포인트 미존재, 품절, 잔액 부족의 무변경, 같은 키 재시도·키 충돌, 같은 사용자 동시 주문 5건, 동시 같은 키 요청 5건을 검증했다. 동시성 검증에서 잔액은 1,000으로 유지됐고 성공 주문·`USE` 이력·`PENDING` Outbox 수가 일치했으며, 같은 키 요청은 각각 한 건만 저장됐다. |
| 2026-07-14 | Issue #5 Manual MySQL 주문 저장 확인 | Manual HTTP/DB Integration | PASS | `docker compose ps`<br>`SPRING_PROFILES_ACTIVE=local ./gradlew bootRun --console=plain`<br>`POST /api/points/charge` (`userId=1`, `amount=10000`)<br>`POST /api/orders` (`userId=1`, `menuId=1`, `quantity=2`)<br>MySQL `orders`, `points`, `point_histories`, `order_events` 조회 | 실행 중인 Compose MySQL에서 Flyway V4가 적용됐다. 주문 API는 201과 `orderId=1`, `quantity=2`, `paymentAmount=9000`, `remainingPoint=62000`, `PAID`를 반환했다. DB에는 주문(`quantity=2`, `order_price=9000`, `PAID`), 포인트 잔액 62,000, 사용 이력(`USE`, 9,000, 62,000), Outbox(`payment_amount=9000`, `PENDING`, `retry_count=0`)가 각각 저장됐다. |
| 2026-07-14 | Issue #5 주문 수량 계약 확장 | Unit/API/DB Integration | PASS | RED: `./gradlew test --tests '*OrderServiceTest' --tests '*OrderCreateResponseTest' --tests '*OrderControllerTest' --console=plain`<br>Focused: 같은 명령 재실행<br>전체: `./gradlew test --console=plain` | `orders.quantity` Flyway V4와 요청·응답 수량을 추가했다. 수량 2 주문에서 총 결제금액 9,000, 잔여 포인트 1,000, `USE` 이력과 Outbox 결제금액 9,000 저장을 검증했다. 수량 0은 서비스와 HTTP 400 공통 응답에서 거부되고, 같은 멱등성 키에 메뉴 또는 수량이 다르면 409로 실패한다. focused 및 전체 테스트는 `BUILD SUCCESSFUL`이었고, Manual MySQL 검증에서 V4 적용과 저장값도 확인했다. |
| 2026-07-14 | Issue #5 주문·결제 완료 메시지 정합성 | API/Unit | PASS | RED: `./gradlew test --tests '*OrderControllerTest' --console=plain`<br>GREEN: 같은 명령 재실행 | HTTP 201 주문·포인트 결제 완료 응답 메시지를 `주문 및 결제가 성공적으로 완료되었습니다.`로 변경했다. 주문 Controller 계약 테스트가 통과했고, README와 API 명세의 201 예시도 같은 메시지로 맞췄다. 기존 200 조회·충전 성공 메시지 `요청이 성공했습니다.`는 유지했다. |
| 2026-07-15 | PR 리뷰 보완: 멱등 재응답·무결성 예외 범위 | Unit | PASS | `./gradlew test --tests '*OrderServiceTest' --tests '*OrderCreateResponseTest' --tests '*GlobalExceptionHandlerTest' --console=plain` | 멱등 재요청의 `remainingPoint`를 현재 포인트가 아닌 주문 `USE` 이력의 차감 후 잔액으로 복원하도록 보완했다. 멱등성 유니크 제약 위반만 409으로 변환하고, 다른 DB 무결성 예외는 일반 서버 오류로 처리하도록 제한했다. |
| 2026-07-15 | Issue #6 Outbox Kafka Publisher | Unit/DB Integration | PASS | RED: `./gradlew test --tests '*OutboxPublisherServiceTest' --console=plain`<br>Focused: 같은 명령 재실행<br>전체: `./gradlew test --rerun-tasks --console=plain` | RED 단계에서 Outbox Publisher·메시지·설정 클래스 부재에 따른 예상 컴파일 실패를 확인했다. focused test 5건과 전체 테스트가 `BUILD SUCCESSFUL`이었다. 주문 서비스가 저장한 `PENDING` 이벤트의 Kafka 발행 후 `SENT`, 실패 후 backoff·실패 횟수·최대 횟수 `FAILED`, 동시 선점 1회, 오래된 `PROCESSING` 회복을 검증했다. |
| 2026-07-15 | Issue #6 Compose Kafka 수동 검증 | Infra Integration/Manual HTTP | PASS | `docker compose up -d --wait`<br>실제 `.env`로 `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun --console=plain`<br>MySQL `SELECT id, order_id, status, retry_count, next_attempt_at, last_error FROM order_events ORDER BY id`<br>`kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic order-paid --from-beginning --max-messages 1 --timeout-ms 5000` | Flyway V5가 MySQL 8.4에 적용됐고, 기존 `PENDING` 이벤트(id=1)가 `SENT`로 전이됐다. `order-paid` 토픽에서 `{"eventId":1,"orderId":1,"userId":1,"menuId":1,"paymentAmount":9000}`를 수신했다. JSON serializer 실행 중 Jackson 의존성 누락을 재현해 `jackson-databind`를 추가한 뒤 재검증했다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-15 | Issue #7 Kafka Consumer와 Redis 랭킹 반영 | Unit/Infra Integration | PASS | `./gradlew test --tests 'com.example.coffeeordersystem.ranking.*' --console=plain`<br>`./gradlew test --console=plain` | Redis 키·Lua 집계 단위 테스트와 임베디드 Kafka DLT 테스트가 통과했다. 동일 eventId의 Lua 반환값 0은 점수 증가 없이 처리되며, Redis 실패는 2회 재시도 후 DLT 전송을 확인했다. 전체 테스트도 BUILD SUCCESSFUL이었다. 실제 Docker Compose Redis에서 Lua 실행 및 주문→Outbox→Consumer 수동 흐름은 실행하지 않았다. |
| 2026-07-15 | Issue #7 Redis Lua 인자 직렬화 회귀 수정 | Unit/Infra Integration | PASS | `./gradlew test --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --console=plain`<br>`./gradlew test --rerun-tasks --console=plain --quiet` | `StringRedisTemplate` Lua 인자를 문자열로 전달하는 회귀 테스트와 Kafka 재시도·DLT 테스트가 통과했다. 전체 테스트도 성공했다. |
| 2026-07-15 | Issue #7 실제 Redis Lua 멱등성 검증 보완 | Redis Integration | PASS | `./gradlew test --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --console=plain` | Testcontainers Redis에서 같은 `eventId`를 두 번 집계해도 ZSET 점수는 한 번만 증가하고, 처리 마커와 일자별 랭킹 키 TTL이 설정됨을 확인했다. |
| 2026-07-15 | Issue #19 Redis Testcontainers 실제 실행 확인 | Redis Integration | PASS | `docker info --format '{{.ServerVersion}} {{.OperatingSystem}}'`<br>`./gradlew test --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --rerun-tasks --console=plain`<br>`build/test-results/test/TEST-com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest.xml` | Docker Desktop 29.5.2가 실행된 상태에서 Testcontainers Redis 테스트가 실행됐다. JUnit 결과는 `tests=1`, `skipped=0`, `failures=0`, `errors=0`이었다. Docker daemon이 없는 환경에서는 전체 단위 테스트의 외부 의존을 막기 위해 이 테스트를 skip하는 정책을 문서화했다. |
| 2026-07-15 | Issue #8 최근 7일 인기 메뉴 Top 3 API | Unit/API/DB Integration/Infra Integration | PASS | `./gradlew test --rerun-tasks --console=plain`<br>`./gradlew test --tests com.example.coffeeordersystem.order.repository.OrderRepositoryTest --console=plain`<br>`docker info --format '{{.ServerVersion}} {{.OperatingSystem}}'`<br>`./gradlew test --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingRedisIntegrationTest --rerun-tasks --console=plain`<br>`build/test-results/test/TEST-*.xml` | 전체 JUnit XML은 `tests=66`, `failures=0`, `errors=0`이었다. 인기 메뉴 ranking·menu·controller focused 테스트와 H2에서 최근 7일 `PAID` 주문의 날짜·메뉴별 집계 테스트가 통과했다. Docker Desktop 29.5.2에서 기존 Redis Lua 테스트 1건과 Issue #8 Redis 합산·복구 테스트 2건도 각각 `skipped=0`, `failures=0`, `errors=0`이었다. Manual HTTP는 미실행이다. |
| 2026-07-15 | Issue #21 Redis 재구성·Consumer 동시성 정합성 | Unit/Infra Integration | PASS | `./gradlew test --tests com.example.coffeeordersystem.global.config.redis.RankingRedisConfigTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingServiceTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --console=plain`<br>`./gradlew test --rerun-tasks --console=plain`<br>`build/test-results/test/TEST-*.xml` | 재구성 잠금 보유 시 DB 집계값만 응답하고 Redis를 수정하지 않는 단위 테스트, Lua가 재구성 잠금 중 재시도 예외를 반환하는 단위·실제 Redis 테스트를 통과했다. `DATA` 상태의 일자별 ZSET 하나가 사라지면 DB 원장으로 7일 전체를 복구하는 회귀 테스트도 통과했다. Docker Testcontainers Redis 테스트는 `skipped=0`, `failures=0`, `errors=0`이었고, 전체 JUnit XML은 `tests=71`, `skipped=0`, `failures=0`, `errors=0`이었다. |
| 2026-07-15 | Issue #22 Redis 랭킹 재구성 잠금 lease 연장 | Unit/Infra Integration | PASS | `./gradlew test --tests com.example.coffeeordersystem.global.config.redis.RankingRedisConfigTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingServiceTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --rerun-tasks --console=plain`<br>`./gradlew test --rerun-tasks --console=plain`<br>`rg -n '<testsuite' build/test-results/test/TEST-com.example.coffeeordersystem.ranking.service.PopularMenuRankingRedisIntegrationTest.xml build/test-results/test/TEST-com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest.xml` | UUID 토큰 일치 시에만 lease 연장·해제가 가능한 Redis Lua와 `0 < renew interval < lock TTL` 설정 검증을 통과했다. lease 연장 실패 또는 소유권 상실 후에는 이후 점수표 쓰기를 중단하고 UUID 값의 이번 재구성 마커만 조건부 정리했다. Testcontainers Redis에서 TTL 2초보다 긴 재구성 중 Consumer가 재시도되고, 완료 후 DB로 복원된 eventId가 중복 증가 없이 ZSET 점수 1로 유지됐다. Redis 통합 XML은 각각 `tests=4`, `skipped=0`, `failures=0`, `errors=0` 및 `tests=2`, `skipped=0`, `failures=0`, `errors=0`이었다. 전체 테스트는 `BUILD SUCCESSFUL`이었다. |
| 2026-07-15 | Issue #22 PR 리뷰 보완: 장기 재구성 Consumer 재시도 | Infra Integration/Unit | PASS | `./gradlew test --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --console=plain`<br>`./gradlew test --rerun-tasks --console=plain` | 일반 Redis 실패는 기존 유한 재시도 뒤 DLT로 이동하고, 재구성 잠금 전용 예외는 설정된 재시도 횟수를 넘어 네 번째 시도에서 성공해 DLT로 이동하지 않음을 Embedded Kafka로 확인했다. 전체 78개 테스트가 성공했다. |
| 2026-07-15 | Issue #15 포인트 충전·주문 교차 동시성 제어 | Unit/DB Integration/Concurrency | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.order.service.OrderServiceTest --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.order.service.OrderServiceTest --tests com.example.coffeeordersystem.order.repository.OrderRepositoryTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain` | RED에서 기존 교차 잠금 정책은 `CHARGE=5,000`, `USE=22,500`인데 잔액 15,000을 남겨 갱신 손실을 재현했다. 충전·주문 모두 사용자 행 `PESSIMISTIC_WRITE`를 먼저 획득하도록 통일한 뒤 focused와 전체 테스트가 `BUILD SUCCESSFUL`이었다. 교차 테스트는 최종 잔액이 초기 잔액 + `CHARGE` 합계 - `USE` 합계와 일치하고 음수가 아님, `CHARGE` 5건·성공 `USE`/주문/Outbox 수 일치, 잔액 부족 교차 주문의 주문·`USE`·Outbox 무변경을 확인했다. 기존 첫 충전 동시성, 주문 단독 동시성, 동일 Idempotency-Key 동시 요청도 focused 실행에 포함돼 통과했다. |
| 2026-07-16 | Issue #9 1회차 k6 주문 부하·정합성 검증 | Load/DB Integration | FAIL | `docker compose up -d --wait`<br>`.env`를 읽기 전용으로 프로세스 환경에 주입한 `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun --console=plain`<br>`scripts/k6/prepare-synthetic-users.sh`<br>`docker run --rm --add-host=host.docker.internal:host-gateway -e BASE_URL=http://host.docker.internal:8080 -e USER_ID=10 -e USERS_FILE=/scripts/build/k6/all-users.json -v /private/tmp/coffee-issue9-k6:/scripts grafana/k6:2.0.0 run /scripts/k6/same-user-order.js`<br>동일 방식으로 `order-load.js`, `order-stress.js`, `order-spike.js` 실행<br>MySQL `points`, `point_histories`, `orders`, `order_events` 집계 조회 | 동일 사용자 5 VU 주문은 HTTP 201 5건, 오류율 0.00%, 평균/P95 332.44/473.02 ms였으나, 100,000P 충전·4,500P 주문 5건 뒤 잔액이 기대 77,500P가 아닌 95,500P였다. `USE` 이력의 `balance_after`도 5건 모두 95,500이었다. 주문·`USE`·Outbox 수는 각 5건으로 일치했다. 다중 사용자 Load(55건, P95 50.40 ms), Stress(172건, P95 171.36 ms), Spike(235건, P95 197.52 ms)는 모두 오류율 0.00%였고 합산 DB 검증에서 `PAID`·`USE`·Outbox 674건 및 잔액 산식이 일치했다. 실제 `.env` 파일은 수정하지 않았다. |
| 2026-07-16 | Issue #9 자동 회귀 | Focused Unit/API/Infra Integration | PASS | Focused: `./gradlew test --tests com.example.coffeeordersystem.point.controller.PointControllerTest --tests com.example.coffeeordersystem.order.controller.OrderControllerTest --tests com.example.coffeeordersystem.menu.controller.MenuControllerTest --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.order.service.OrderServiceTest --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain` | focused 테스트와 전체 회귀가 모두 `BUILD SUCCESSFUL`이었다. 전체 회귀는 `2m 33s`에 완료됐다. k6에서 재현한 실제 MySQL 동일 사용자 잔액 유실은 자동 회귀에 포함되지 않은 별도 결함이며, 이 결과로 상쇄하지 않는다. |
| 2026-07-16 | Issue #9 1회차 HTTP 계약·멱등성 DB 검증 | API/Manual HTTP/DB Integration | PASS | `.env`를 읽기 전용으로 프로세스 환경에 주입한 `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun --console=plain`<br>`docker run --rm --add-host host.docker.internal:host-gateway -v /private/tmp/coffee-issue9-http-client:/workdir jetbrains/intellij-http-client -D api-contract-verification.http --report`<br>`node --check k6/lib/order-scenario.js k6/same-user-order.js k6/order-load.js k6/order-stress.js k6/order-spike.js`<br>MySQL `orders`, `point_histories`, `order_events`, `points` 조회 | `http/api-contract-verification.http`의 요청 5개와 response handler assertion이 `RUN SUCCESSFUL`(JUnit `tests=15`, `failures=0`, `errors=0`, `skip=0`)이었다. PR 리뷰 보완 후 인기 메뉴 최소 1건 assertion을 포함해 같은 파일을 재실행했고 다시 통과했다. 최초 주문·같은 키 재시도의 `orderId=1223`, `remainingPoint=218500`이 동일했고, 생성 주문의 주문·`USE` 이력·Outbox는 각각 1건이었다. 잔액 부족은 400 `포인트가 부족합니다.`이며 `data`·내부 `code`가 없었다. 실제 `.env` 파일은 수정하지 않았다. |
| 2026-07-16 | Issue #26 동일 사용자 포인트 차감 유실 수정 | DB Integration/Concurrency | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.order.service.OrderPointConcurrencyMySqlIntegrationTest --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.order.service.OrderServiceTest --tests com.example.coffeeordersystem.order.service.OrderPointConcurrencyMySqlIntegrationTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain` | 실제 MySQL `REPEATABLE-READ`에서 일반 조회 스냅샷이 사용자 잠금 대기 뒤에도 유지돼 5건 주문의 잔액 95,500P·동일 `USE.balance_after`를 재현했다. `users -> points` locking read와 사용자 잠금 뒤 주문·`USE` 이력 locking read로 보완했다. Testcontainers MySQL 8.4의 4건(서로 다른 키 주문, 충전·주문 교차, 같은 키 재시도, 잔액 부족) 및 focused 25건, 전체 84건은 skip·failure·error 0이었다. |
| 2026-07-16 | Issue #28 2회차: PR #27 수정 반영 재검증 | API/Load/DB Integration | PASS | `.env`를 읽기 전용으로 환경에 주입한 `SPRING_PROFILES_ACTIVE=local ./gradlew bootRun --console=plain`<br>`docker run --rm --add-host host.docker.internal:host-gateway -v /private/tmp/coffee-http-verification:/workdir jetbrains/intellij-http-client -D api-contract-verification.http --report`<br>매 시나리오별 `scripts/k6/prepare-synthetic-users.sh` 및 Docker `grafana/k6:2.0.0 run --summary-trend-stats 'avg,min,med,max,p(90),p(95),p(99),count' --summary-export <scenario>-summary.json`으로 `same-user-order.js`, `order-load.js`, `order-stress.js`, `order-spike.js` 실행<br>MySQL 사용자별 잔액·`CHARGE`/`USE`·`PAID`·Outbox 집계, `Innodb_deadlocks`, `performance_schema.data_lock_waits` 조회 | HTTP 전체는 JUnit `tests=15`, failure/error/skip 0이었다. safe k6는 동일 사용자 5건(P95/P99 68.13/69.38ms), Load 54건(47.60/63.15ms), Stress 208건(44.13/93.91ms), Spike 298건(35.26/94.23ms)으로 모두 오류율 0.00%·threshold PASS였다. 모든 synthetic 사용자에서 잔액 산식, 음수 방지, `PAID`=`USE`=Outbox를 확인했다. 실행 완료 후 `Innodb_deadlocks=0`, 현재 `data_lock_waits=0`을 확인했으며, 실행 중 잠금 대기와 애플리케이션 재시도 횟수는 미측정이다. |
| 2026-07-16 | Issue #30 주문 시각 기준 Redis 랭킹 집계 | Unit/Infra Integration | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingServiceTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingRedisIntegrationTest --rerun-tasks --console=plain` | RED는 `orderedAt` 없는 기존 이벤트 생성자 컴파일 실패를 확인했다. Focused 22건은 `skipped=0`, `failures=0`, `errors=0`이었다. 자정 전 주문을 자정 후 Consumer Clock에서 처리해도 주문일 ZSET·상태 키를 선택하고, 실제 Redis Lua에서 지연 소비·동일 eventId 재소비가 주문일 점수를 1회만 증가시키며 다음 날 키를 만들지 않음을 확인했다. Outbox 메시지의 주문 시각 전달, Kafka JSON 직렬화·역직렬화, 일반 Redis 실패 DLT 및 재구성 잠금 재시도, Redis 재구성·복구 회귀도 포함했다. |
| 2026-07-16 | Issue #30 전체 Gradle 회귀 | Unit/API/DB Integration/Infra Integration | FAIL | `./gradlew test --rerun-tasks --console=plain`<br>`sh gradlew test --rerun-tasks --console=plain --no-daemon` | 두 번째 실행은 1분 58초 뒤 `:test`에서 `build/test-results/test/binary/in-progress-results-generic.bin`의 `NoSuchFileException`으로 실패했다. Gradle daemon 로그상 테스트 결과 파일이 실행 중 사라졌으며, 전체 회귀 PASS는 확인하지 못했다. |
| 2026-07-16 | Issue #30 전체 Gradle 회귀 재실행 | Unit/API/DB Integration/Infra Integration | PASS | `./gradlew test --rerun-tasks --console=plain` | 다른 Gradle 실행이 없는 상태에서 단일 실행으로 재검증했다. `BUILD SUCCESSFUL`(1분 27초), JUnit XML 26개 합계 `tests=84`, `skipped=0`, `failures=0`, `errors=0`이었다. |
| 2026-07-17 | Issue #32 Outbox 처리 timeout 중복 선점 방지 | Unit/DB Integration/Infra Integration | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest.Kafka_발행이_처리_제한_시간을_넘어도_유효한_선점은_다른_Publisher가_회수하지_않는다 --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest --tests com.example.coffeeordersystem.outbox.config.OutboxPublisherPropertiesTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --rerun-tasks --console=plain`<br>전체 재실행: `./gradlew test --rerun-tasks --console=plain` | RED에서는 지연된 Kafka 발행 중 다른 Publisher가 stale 회수해 첫 Publisher 완료가 선점 토큰 불일치로 실패했다. lease 갱신 뒤 focused 11건은 `skipped=0`, `failures=0`, `errors=0`이었다. 여기에는 처리 제한 시간 초과 지연 발행의 단일 발행, lease 갱신이 멈춘 `PROCESSING` 회복, 동시 선점, 실패 backoff·최대 횟수 `FAILED`, Embedded Kafka DLT, 실제 Redis 동일 `eventId` 점수 무증가가 포함된다. 전체 첫 단독 실행은 `in-progress-results-generic.bin`의 `NoSuchFileException`으로 실패했으나, 다른 Gradle 실행 없이 표준 명령으로 재실행해 `BUILD SUCCESSFUL`(1분 8초), JUnit XML 27개 합계 `tests=86`, `skipped=0`, `failures=0`, `errors=0`을 확인했다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-17 | Issue #32 독립 리뷰 보완: 동기 Kafka send lease 갱신 | Unit/DB Integration/Infra Integration | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest.Kafka_발행_호출이_처리_제한_시간을_넘어도_유효한_선점은_다른_Publisher가_회수하지_않는다 --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest --tests com.example.coffeeordersystem.outbox.config.OutboxPublisherPropertiesTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain` | 독립 리뷰에서 `KafkaTemplate.send()` 호출 자체가 block되면 기존 heartbeat가 시작하지 않는 경로를 지적했다. RED에서 해당 경로의 선점 토큰 불일치를 재현했다. `send()` 호출을 별도 Future로 분리해 호출·완료 대기 전 기간 lease를 갱신하고, 1초 미만 `processingTimeout`을 거부하며, lease 상실 완료 처리는 상태 무변경으로 처리했다. Focused 13건과 전체 JUnit XML 27개 합계 `tests=88`, `skipped=0`, `failures=0`, `errors=0`이 통과했다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-17 | Issue #34 Outbox Kafka send executor liveness | Unit/DB Integration/Infra Integration | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest.Kafka_send_작업이_거절되면_lease를_연장하지_않고_기존_실패_재시도_경로로_전환한다 --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest --tests com.example.coffeeordersystem.outbox.config.OutboxPublisherPropertiesTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain` | 대기열 없는 Spring 관리 executor를 추가했다. executor 거절과 작업 미시작 Future는 Kafka 호출 없이 기존 실패·backoff 경로로 전환하며, 실행 중 Kafka send는 시작 플래그로 lease를 계속 갱신한다. focused 테스트와 전체 회귀는 `BUILD SUCCESSFUL`(1분 18초)이었다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-17 | Issue #35 Kafka 성공 후 Outbox DB 완료 상태 실패 처리 | Unit/DB Integration/Infra Integration | PASS | RED: `./gradlew test --tests 'com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest.Kafka_발행_성공_후_SENT_기록에_실패하면_실패_재시도_경로로_전이하지_않고_stale_회복_뒤_재발행한다' --rerun-tasks --console=plain` (예상 assertion 실패)<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain --no-daemon`<br>JUnit XML: `build/test-results/test/TEST-*.xml` | RED에서 Kafka send 완료 뒤 `markSent()` 예외가 기존 외부 catch로 전달돼 `markFailed()`를 거치는 문제를 재현했다. 수정 후 focused는 `BUILD SUCCESSFUL`(18건)이었다. 완료 기록 실패는 `PROCESSING`·token·retry count 0을 보존하고 stale recovery 뒤 동일 eventId를 재발행해 `SENT`로 전이했다. stale recovery로 새 token이 선점한 뒤 이전 Publisher의 완료 처리는 거부됐다. 기존 Kafka 실패 backoff·최대 횟수 초과 `FAILED`, 실제 Redis Lua의 같은 eventId 점수 무증가, Embedded Kafka Consumer 재시도·DLT 회귀를 포함했다. 전체 JUnit XML 27개는 `tests=92`, `skipped=0`, `failures=0`, `errors=0`이었다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-18 | Issue #37 API 입력 검증·예외 처리 focused·전체 회귀 | Unit/API/DB Integration/Infra Integration | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.order.controller.OrderControllerTest --tests com.example.coffeeordersystem.global.exception.GlobalExceptionHandlerTest --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.order.controller.OrderControllerTest --tests com.example.coffeeordersystem.order.service.OrderServiceTest --tests com.example.coffeeordersystem.global.exception.GlobalExceptionHandlerTest --rerun-tasks --console=plain`<br>전체: `sh gradlew test --rerun-tasks --console=plain --no-daemon` | RED에서 101자 키, 405, 415, 500 처리 테스트 4건이 실패했다. 수정 후 focused는 `BUILD SUCCESSFUL`이었다. MockMvc로 101자 키의 서비스 미호출(주문·포인트·사용 이력·Outbox 저장 경로 미진입), 405/415 공통 오류, 내부 상세 없는 500 응답과 ERROR 로그를 확인했고, 서비스 통합 테스트로 100자 키의 주문·포인트 차감·사용 이력·Outbox 저장을 확인했다. 전체 회귀의 JUnit XML 27개 합계는 `tests=101`, `skipped=0`, `failures=0`, `errors=0`이었다. |
| 2026-07-18 | Issue #39 환경별 테스트 seed 분리 | DB Integration/API | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.global.config.flyway.EnvironmentSeedFlywayIntegrationTest --rerun-tasks --console=plain`<br>Flyway focused: `./gradlew test --tests com.example.coffeeordersystem.global.config.flyway.EnvironmentSeedFlywayIntegrationTest --rerun-tasks --console=plain`<br>API focused: `./gradlew test --tests com.example.coffeeordersystem.point.controller.PointControllerTest --tests com.example.coffeeordersystem.order.controller.OrderControllerTest --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.order.service.OrderServiceTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain --no-daemon` | RED에서 신규 로컬이 아닌 DB에도 V3가 적용되어 `users.id=1`이 생성됨을 재현했다. 수정 후 MySQL Testcontainers 3건은 신규 로컬 DB의 V1/V2·로컬 V3 및 사용자 1, 신규 로컬이 아닌 DB의 V1/V2·메뉴 5건과 사용자 1 미생성, 기존 V3 이력 DB의 `validate`/`migrate` 호환과 기존 사용자 보존을 확인했다. 포인트·주문 focused와 전체 회귀는 `BUILD SUCCESSFUL`이었고, 전체 JUnit XML 28개 합계는 `tests=104`, `skipped=0`, `failures=0`, `errors=0`이었다. 실제 `.env`는 수정하지 않았다. Embedded Kafka 종료 중 임시 로그 디렉터리 경고는 있었으나 Gradle 종료 코드는 0이었다. |
| 2026-07-19 | Issue #39 독립 리뷰 보완 | Unit/DB Integration/Infra Integration | PASS | `./gradlew test --tests '*EnvironmentSeedFlywayIntegrationTest' --rerun-tasks --console=plain --no-daemon`<br>`./gradlew test --rerun-tasks --console=plain --no-daemon` | 전역 `versioned:missing` 설정을 제거하고, 적용 이력에서 V3 하나만 누락된 경우에만 tolerant Flyway validate/migrate를 사용하도록 제한했다. V4/V5 공통 migration 적용 이력과 V3 외 누락 versioned migration 거부 테스트를 추가했다. Flyway focused 4건과 전체 빌드는 모두 `BUILD SUCCESSFUL`이었고, 전체 JUnit XML은 `tests=105`, `skipped=0`, `failures=0`, `errors=0`이었다. 실제 `.env`는 수정하지 않았다. Embedded Kafka 종료 중 임시 로그 디렉터리 경고가 있었으나 Gradle 종료 코드는 0이었다. |
| 2026-07-19 | Issue #39 용어 정정 | DB Integration/API | PASS | `./gradlew test --tests '*EnvironmentSeedFlywayIntegrationTest' --rerun-tasks --console=plain --no-daemon`<br>`./gradlew test --rerun-tasks --console=plain --no-daemon` | 문서·테스트 이름의 `비로컬` 표현을 `로컬이 아닌`으로 정정했다. focused 테스트와 전체 빌드는 모두 `BUILD SUCCESSFUL`이었고, 전체 JUnit XML은 `tests=105`, `skipped=0`, `failures=0`, `errors=0`이었다. Embedded Kafka 종료 중 임시 로그 디렉터리 경고가 있었으나 Gradle 종료 코드는 0이었다. |
| 2026-07-19 | Issue #41 포인트 잔액 오버플로 방지 | Unit/API/DB Integration | PASS | RED: `./gradlew test --tests com.example.coffeeordersystem.point.entity.PointEntityMappingTest --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.point.controller.PointControllerTest --rerun-tasks --console=plain`<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.point.entity.PointEntityMappingTest --tests com.example.coffeeordersystem.point.service.PointServiceTest --tests com.example.coffeeordersystem.point.controller.PointControllerTest --tests com.example.coffeeordersystem.order.service.OrderServiceTest --tests com.example.coffeeordersystem.order.service.OrderPointConcurrencyMySqlIntegrationTest --rerun-tasks --console=plain`<br>전체: `./gradlew test --rerun-tasks --console=plain` | RED는 `POINT_BALANCE_OVERFLOW`가 없어 테스트 컴파일에 실패했다. 수정 후 focused JUnit XML은 엔티티 3건, 포인트 서비스 9건, 포인트 Controller 10건, 주문 서비스 15건, MySQL 동시성 5건으로 합계 42건이며 `skipped=0`, `failures=0`, `errors=0`이었다. 최대값 경계 충전 성공, 초과 충전의 전용 오류·HTTP 400, 초과 뒤 잔액·`CHARGE` 이력 무변경, 실제 MySQL 동시 충전에서 성공 1건·초과 거절 1건과 상한 유지를 확인했다. 전체 JUnit XML 28개 합계는 `tests=110`, `skipped=0`, `failures=0`, `errors=0`이었고, 후속 `./gradlew test --console=plain`은 `BUILD SUCCESSFUL`로 종료됐다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-19 | Issue #43 Outbox 배치 대기 이벤트 lease 만료 방지 | Unit/Infra Integration | PASS | RED: `./gradlew test --tests 'com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest.앞선_Kafka_전송이_처리_제한_시간을_넘어도_같은_배치의_대기_이벤트는_다른_Publisher가_재선점하지_않는다' --rerun-tasks --console=plain`<br>GREEN: 동일 단일 테스트 명령<br>Focused: `./gradlew test --tests com.example.coffeeordersystem.outbox.service.OutboxPublisherServiceTest --tests com.example.coffeeordersystem.outbox.config.OutboxPublisherPropertiesTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --rerun-tasks --console=plain`<br>전체: `sh gradlew test --rerun-tasks --console=plain --no-daemon` | RED는 앞선 Kafka 전송 지연 중 뒤쪽 대기 이벤트가 stale recovery로 재선점되어 Kafka `send()`가 2회 호출되는 assertion 실패를 재현했다. 토큰 단위 lease 갱신 뒤 단일 GREEN과 focused는 `BUILD SUCCESSFUL`; focused JUnit XML 4개 합계 `tests=18`, `skipped=0`, `failures=0`, `errors=0`이었다. Publisher 테스트는 배치 대기 이벤트 보호, 같은 이벤트의 다중 Publisher 단일 발행, 갱신 중단 뒤 stale recovery, Kafka 성공·실패·`SENT` 기록 실패·토큰 불일치를 검증한다. 연결된 Embedded Kafka/Redis 테스트도 focused에 포함했다. 전체는 `BUILD SUCCESSFUL`(1분 32초)로 종료됐고 JUnit XML 28개 합계 `tests=111`, `skipped=0`, `failures=0`, `errors=0`이었다. 실제 `.env`는 수정하지 않았다. |
| 2026-07-19 | Issue #45 랭킹 재구성 독립 snapshot 보장 | Unit/DB Integration/Infra Integration | PASS | `./gradlew test --rerun-tasks --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingServiceTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.service.PopularMenuRankingTransactionIsolationIntegrationTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationServiceTest --tests com.example.coffeeordersystem.ranking.redis.RedisRankingAggregationRedisIntegrationTest --tests com.example.coffeeordersystem.ranking.consumer.OrderPaidRankingConsumerTest --tests com.example.coffeeordersystem.ranking.consumer.RankingConsumerRetryDltIntegrationTest --console=plain` | 20개 테스트가 모두 통과했다. 실제 MySQL에서 외부 `READ_COMMITTED` 트랜잭션 안의 랭킹 재구성이 `REQUIRES_NEW` 읽기 전용 `REPEATABLE_READ` snapshot을 열고, 두 조회 사이 별도 트랜잭션으로 커밋된 주문·Outbox를 점수와 marker 어느 쪽에도 혼합하지 않음을 확인했다. Redis 재구성 잠금·lease·Lua marker·점수표와 Consumer 재시도 회귀도 통과했다. |
| 2026-07-19 | Issue #45 전체 Gradle 회귀 | Unit/API/DB Integration/Infra Integration | PASS | `./gradlew cleanTest --console=plain`<br>`./gradlew --no-daemon test --rerun-tasks --console=plain` | 기존 손상된 Gradle test-result 직렬화 데이터로 발생한 `EOFException` 뒤 `cleanTest`로 생성물을 정리하고 daemon 없이 재실행했다. 전체 29개 테스트 클래스가 실행됐으며 JUnit XML 결과는 failures=0, errors=0, skipped=0이었다. |
| 2026-07-19 | Issue #47 인기 랭킹 원장 정합성·원자 snapshot | Unit/DB Integration/Infra Integration | PASS | `./gradlew test --tests 'com.example.coffeeordersystem.ranking.*' --no-daemon --rerun-tasks --console=plain`<br>`./gradlew test --no-daemon --rerun-tasks --console=plain`<br>`docker info --format '{{.ServerVersion}}'`<br>실제 집계 SQL의 `docker exec ... mysql ... -e "EXPLAIN SELECT DATE(o.ordered_at), o.menu_id, COUNT(o.id) ... GROUP BY DATE(o.ordered_at), o.menu_id"`<br>`git diff --check` | 랭킹 focused 26건과 전체 120건이 `skipped=0`, `failures=0`, `errors=0`으로 통과했다. Testcontainers Redis·MySQL과 Embedded Kafka/DLT가 실제 실행됐다. EXPLAIN은 기존 `idx_orders_status_ordered_at`을 사용하고 `Using index condition; Using temporary`를 보여 새 인덱스나 Flyway migration은 추가하지 않았다. Redis Consumer Lua의 marker·ZSET·일자별 count·상태·TTL 원자 기록, duplicate eventId 무증가, Redis 7일 snapshot/DB PAID count 비교, EMPTY 날짜 count 0 복구, stale Redis·Outbox/Kafka 지연·다중 서버 lock·Asia/Seoul 경계를 회귀 검증했다. 실제 `.env`는 존재하지 않았고 `.env.example`도 수정하지 않았다. 전체 실행 중 Embedded Kafka/Testcontainers 종료 시 임시 디렉터리·연결 종료 경고가 있었지만 Gradle 종료 코드와 JUnit 결과는 성공이었다. |

완료라고 기록할 때는 실제 실행한 명령과 결과만 작성한다. 실행하지 않은 검증은 `PARTIAL` 또는 미기록으로 남긴다.
